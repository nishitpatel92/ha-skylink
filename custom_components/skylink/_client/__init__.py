"""Skylink Orbit protocol client.

Pure Python — zero `homeassistant` imports. Designed to be extractable to a
standalone PyPI package (`skylink-orbit-client`) without code changes.

Public API lives on `OrbitClient` (see `client.py`).
"""
